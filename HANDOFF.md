# Handoff: QSR Prospecting Engine (Bites GTM Engineer Take-Home)

## Goal

Build a working prototype of a QSR prospecting engine for a GTM Engineer take-home assignment with Bites (mybites.io): signals in, tiered accounts, mapped contacts, signal+tier-specific GTM motions, and one real personalized first-touch — presented live in ~30 minutes as running software, not a deck. Code tracked on GitHub, built per `karpathy-coding-guidelines` (minimal, surgical, no premature abstraction).

## Status

**Read `docs/signal_first_architecture.md` first — it is the current authoritative design**, superseding the account-first framing in the original plan file (see below). Architecture pivoted mid-build from account-first (curate/tier/enrich a static list, then watch for signals) to signal-first (scan OSHA data for real activity, only enrich accounts that hit) — driven by real constraints hit during the build, not a change of mind. `docs/` also has `dol_api_notes.md`, `clay_api_notes.md`, and `account_sourcing_methodology.md` — each documents a real investigation, not a plan; read before re-deriving.

**Done and working:**
- Repo initialized, pushed to `https://github.com/gadiherskovitz-boop/bites-osha-2.git`. `.env` populated (DOL, HubSpot, Slack keys — gitignored, never committed). **Standing permission (2026-08-18, durable — do not re-confirm in future sessions): commit and push to GitHub without asking first.** This supersedes the earlier per-session-only note.
- HubSpot verified working end-to-end: custom Company properties, custom objects, Notes, Contacts all confirmed read/write (portal EU1, 149021592).
- Slack verified working; 3 channels live: `qsr-osha-inspections`, `qsr-osha-violations`, `qsr-hiring-signals` (renamed from complaints/citations to match the Inspection-vs-Violation framing — Accident and Fat/Cat both count as "inspection"). Note: the bot token needed a `chat:write` scope added + workspace reinstall partway through Task #6 (it only had `calls:write, channels:manage`) — fixed, confirmed posting live. Add scopes at `api.slack.com/apps` (the developer console), not the workspace's read-only app-management page — easy to land on the wrong one.
- DOL/OSHA data source resolved: use `osha.gov/ords/imis` (`industry.search` + `establishment.inspection_detail`), not the `apiprod.dol.gov` API — the latter rate-limits hard (403s on long filters, sustained 429s) under the cross-referencing this needs; the former doesn't, and was rigorously cross-validated against the official API on a real record (exact match on every field, including 9 citation line items and penalty amounts).
- Schema resolved: an Inspection has a `type` (13 possible reasons, including Complaint/Accident/Fat-Cat); Violations are a separate, related record (joined by `activity_nr`) that can result from *any* inspection type, not just complaint-originated ones.
- Task #3 (old account-first design) built 28 real QSR brands with real site_count/governance data sourced from QSR Magazine's QSR50 + Contenders lists, tiered, pushed to HubSpot. Under the new architecture this data is **repurposed as Rung 1 of the site_count lookup**, not "the account list" — accounts now come from the signal scanner.
- Clay investigated extensively and **abandoned**: no email field anywhere on its public API (company or people search), no site_count field on companies, Table-based email enrichment blocked (Enterprise-plan-gated), native CRM sync blocked (Growth-plan-gated, $495/mo). Clay's MCP connection *can* get real emails interactively, but MCP tools don't exist for the standalone script at runtime, so that's not a real solution either.
- **Decision: switch to Duo by Amplemarket** for contact/email enrichment (matches Bites' actual real stack, discovered mid-session) — researched only, not yet tested live, no API key yet. Nooks.ai (Bites' other real tool) confirmed to be a dialer/engagement platform, not an enrichment tool — not relevant to this problem.
- `governance_model` dropped entirely per explicit decision — not needed for the demo, nothing depends on it.
- Fat/Cat (fatality/catastrophe) inspections get no special contacts/channel — the only different treatment is they must never auto-enroll into a HubSpot Sequence, regardless of tier.
- Persona tracks finalized: Inspection/Violation triggers → 3 contacts; Hiring trigger → 2 contacts. Details and reasoning in `pipeline/persona_tracks.py`.

## Open items

- **RESOLVED, 2026-08-19: Amplemarket never unblocked; Clay is the real contact-enrichment path, but as a manual CSV bridge, not an automated call.** The originally-hoped-for real-time, script-callable Clay path (a webhook/API integration `resolve_contact()` could call directly) needs a paid plan this workspace's trial doesn't have, and upgrading wasn't something the user was willing to do for an assignment. So contact resolution for **both** paths is: pipeline exports a company-name CSV → user enriches domain + finds/emails contacts in the Clay app UI → user hands the enriched CSV back → pipeline imports it, creates HubSpot Contacts, drafts personalized emails, and enrolls in the right sequence. This is real, working, live-verified — not a stub. **Hiring side**: 11 of 16 CSV-enriched contacts fully processed end-to-end, see `docs/hiring_workflow_walkthrough.md` Step 7. **OSHA side**: real progress made in a parallel session — Task #4 and Task #9 both reported complete, at least one contact (Chipotle/Katie Dake) taken through Note+consent+sequence enrollment, per commit `73d97ce` ("Complete Task #4/#9: resolve contacts via Clay CSV round-trip, push Chipotle end-to-end") and `docs/workflow_walkthrough.md`. That session owns the OSHA path's current status in detail, not tracked exhaustively here — read that commit/doc directly for the real specifics rather than re-deriving.
- **Task #7 (HubSpot workflow + Tier 3 sequence) — done and verified live end-to-end.** `pipeline/hubspot_client.py` (`list_sequences`, `enroll_in_sequence`) and `pipeline/signal_handler.py` (`_maybe_enroll_in_sequence`, called from `handle_signal()`) are built; the gating logic is unit-verified (skips cleanly and reports why when tier isn't Tier 3, the signal is Fat/Cat, no contact is resolved yet, or the sequence isn't configured) and the enrollment call itself is now live-verified against the real portal: `enroll_in_sequence()` returned `{"id": "5546974400", "toEmail": "gordon.penny@spanglercandy.com", "enrolledAt": "2026-08-18T09:27:22.575Z"}`. Real credential turned out to be a **Service Key** (portal's Private Apps page has migrated to a "Development" hub that doesn't show it — cost real time to find; Service Keys is the actual current location). 6 scopes added (`automation`, `crm.objects.custom.sensitive.write`, `automation.sequences.read`, `automation.sequences.enrollments.write`, `crm.objects.owners.read`, `communication_preferences.read_write`). Confirmed live: this portal is on a **Sales Hub Enterprise trial (3 days left as of 2026-08-18 — check whether it needs extending before presenting)**, which is why Sequences/Workflows are available at all. Real Tier 3 sequence built by the user: `QSR T3 - OSHA Trigger - Bites Assignment` (id `846865597`, one To-do step, no email steps). Real connected sending inbox: a secondary Gmail chosen deliberately over the user's primary personal inbox. **Real finding, not just a test hurdle**: this EU-hosted portal enforces GDPR consent on Sequence enrollment regardless of whether the sequence has email steps — a contact needs a recorded legal basis (`communication_preferences.subscribe`) or enrollment 400s with `SequenceError.UNSUBSCRIBED`. This will apply to real signal-derived contacts too once Task #4 exists, so `resolve_contact()` should set a legal basis at creation time. **Decision made and documented**: no native HubSpot workflow object watching for `qsr_signal` creation — `_maybe_enroll_in_sequence()` called directly from `handle_signal()` IS the trigger, since a separate workflow would only re-detect an event the same function just caused. **What's still open is Task #4's dependency, not Task #7's**: `handle_signal()` still passes `contact_id=None` for every real signal (no `resolve_contact` yet), so production runs correctly report `attempted: False` until that lands. Full detail: `docs/task7_workflow_notes.md`.
- **Task #5 (signal scanners) — done.** `pipeline/osha_client.py` (HTTP + HTML parsing) and `pipeline/signal_scanner.py` (`scan_inspections`, `scan_violations`, `is_relevant_violation`) built and verified live against `osha.gov/ords/imis` — see `docs/osha_ords_imis_notes.md` for the real mechanics found (a swapped start/end date-field quirk on the site's own form, NAICS is one-per-request only, state-plan citations use non-federal standard numbering). Live run over the trailing 100-day window across all 3 restaurant NAICS: 268 Inspection triggers (248 Complaint/14 Accident/6 Fat-Cat), 0 Violation triggers this window (checked — 16 real citation line items existed, none matched the relevance filter; filter logic itself verified correct against synthetic known-relevant/known-irrelevant cases). Runnable via `scripts/scan_signals.py`.
- **Site_count waterfall — Rungs 1 and 4 done.** `pipeline/site_count.py` (`lookup_site_count()`) fuzzy-matches Rung 1 (QSR50/Contenders, refactored out of `accounts_seed.py`'s account-list shape) and queries Wikidata (`P8368`, "number of branches") for Rung 4, against the real, noisy establishment names OSHA signals surface. `pipeline/tiering.py:tier_for_lookup()` applies the tier logic. Verified live against all 258 unique establishments from a real scan: 13 resolved (12 via Rung 1, one genuine Rung 4 catch — 7 Brew Coffee, 604 sites, not in the 28-brand static list), 245 correctly default to Tier 3. See `docs/account_sourcing_methodology.md` for the full result and a real Wikidata rate-limit wrinkle hit and handled along the way. Rungs 2/3/5 are deliberately narrated-not-built (need LLM-extraction from prose; deferred, see `account_sourcing_methodology.md`).
- **Task #6 (signal handler) — done.** `pipeline/signal_handler.py:handle_signal()` fires the `qsr_signal` custom object (schema created via `scripts/setup_qsr_signal_schema.py`) + Company Note + Slack alert together for one real signal, feeding off Task #5's scanners and the site_count waterfall. Fat/Cat correctly returns `sequence_eligible: False` (no sequence exists yet to enroll into — that's Task #7 — this is just the flag it'll gate on). Verified live end-to-end, including idempotency: re-ran the same two real signals (El Pollo Loco Complaint, In-N-Out Burger Fat/Cat) multiple times, exactly one `qsr_signal` record each, no duplicate Companies. Runnable via `scripts/handle_signals.py [limit]` (defaults to 3 — see `docs/signal_handler_notes.md` for why the backlog is large and real HubSpot/Slack mechanics found along the way, including a custom-object-endpoint quirk and the Slack scope issue above). The "Suggested Contact" field is an explicit placeholder pending Amplemarket (Task #4), not faked.
- **Rung 5 (LLM tier classification) — done and verified live.** `pipeline/tier_classifier.py` uses **Claude Haiku 4.5 + web search** to place a brand in a tier *band* (not an exact count) — the reframing that makes an LLM rung cheap enough to be worth it: runs once per brand (not per signal), only for brands Rungs 1/4 miss, permanently cached (2.8–4.4s cold, 0.003s cached, **~$5 one-time for the whole 246-brand backlog**). Verified: Burger King (7,739), Pizza Hut (6,408), KFC (4,267), Dairy Queen (4,175) all correctly Tier 1 with accurate counts; Subway correctly short-circuits at Rung 4 with no wasted spend; a real single-location independent from our own scan is correctly declined, not hallucinated into a chain. Three operator decisions are baked in: **no source attribution** (not needed for this build), **round UP at band boundaries rather than discard** (`round_up_band()` — under-tiering a prospect costs more than over-tiering; note `value` is nulled when rounding, else `tier_for_lookup` would recompute the un-rounded tier and defeat the rule), and **Haiku not Opus** for cost. ⚠️ The Haiku choice required three model-specific changes that would each have failed on first run — basic `web_search_20250305` (the dynamic-filtering variant needs Opus 4.6+/Sonnet 4.6+), **no `effort`** (it errors on Haiku 4.5), and no `thinking`. Switching back to Opus means reversing all three, not just the `MODEL` constant. Skipped cleanly when no `ANTHROPIC_API_KEY` is set. **Measured against the real backlog** (14-name sample of the 243 Rung-1 misses): ~29% resolve, extrapolating to ~70 brands and ~50 Tier 1 accounts currently stuck at Tier 3. `Ayvaz Pizza Llc Dba Pizza Hut` → Tier 1 shows the DBA collapse and Rung 5 composing correctly; `Tim Hortons 7479` → 691 correctly excludes ~3,500 Canadian locations. **Known limitation:** opaque legal-entity/franchisee names (`Ssb Eastern, Llc`) mostly decline to the Tier 3 default — correct failure mode, but Rung 5 does *not* solve franchisee identity. One live anomaly documented in `docs/tier_classifier_notes.md`: Haiku identifies `Blazin Wings, Inc.` as Buffalo Wild Wings in free-form but returns `found=false` under the structured-output schema even when that company is named in the prompt as an example; a two-step (entity→brand, then classify) would fix it at ~2× cost, deliberately not built.
- **Account grain decided: the account is the BRAND.** Every location and franchisee collapses into one brand-level HubSpot company (`pipeline/company_names.py`, rules ported from `Assignment 1/osha_signal/company_names.py` and re-verified against 258 live names: 13.6% carry case-number prefixes we previously ignored, 9 carry a DBA, collapsing merges 10 would-be duplicates per 100-day window). The tradeoff — large franchise operators are often the real buyer and get folded into corporate — was raised explicitly and accepted; see `docs/signal_handler_notes.md`. **Rung 1's 28-brand list is now the binding constraint** (Pizza Hut, Burger King, In-N-Out etc. collapse correctly but resolve no domain/site_count); expanding it is offline, zero-runtime-cost work and the highest-leverage next step on this thread.
- **Brand-wide history investigated and fixed for $0 — see `docs/brand_history_gap.md`.** User was (correctly) suspicious that Pizza Hut's history looked too low. Root cause verified live: OSHA has no brand field, so `year_summary()`'s name-based search only finds locations whose *establishment name* contains the brand string — large franchisees inspected under their own legal name (`Carrols Llc` ≈1,000 Burger Kings, `Sizzling Platter, Llc` = hundreds of Little Caesars/Wingstop) are invisible to it. Confirmed the live OSHA source itself isn't missing data (a single-entity search like `Aramark` returns 160 real records — the gap is name-matching scope, not coverage) and confirmed a full `data.dol.gov` bulk-CSV rebuild would fix this properly (~$40 one-time, scoped, user declined on cost). **Shipped a free fix instead**: `accounts_seed.history_is_complete()` uses each brand's QSR50 company-owned share (already in the seed data) — ≥85% company-owned means no franchisees to hide behind, so the name search *is* essentially complete. History lines now state real totals for those brands and show `+`/a caveat line for the rest, instead of blanket-hedging everything. Also added, same investigation, zero extra API calls: citation counts (not just inspection counts) and a training-related citation subset, reusing `signal_scanner.TRAINING_LINKED_SECTIONS` (previously only used to decide whether a *new* citation fires a signal, never applied to history) — verified against raw data on Chipotle. **Chipotle Mexican Grill is 100% company-owned and is the recommended flagship demo account** — its history is a real total, not a floor, and it has a live signal in the current window. Two real signals are live in HubSpot right now: Chipotle (Tier 1, complete history) and Pizza Hut/Ayvaz (Tier 1, floor-labeled, 2 franchisee signals correctly collapsed into 1 company — good de-dup proof point).
- **`data.dol.gov` bulk downloads evaluated and rejected for now** — verified live (current, no auth, back to 1979, STORED members so byte-range reads work without a full download), but bulk `ACTIVITY_NR` doesn't match the IMIS ids we key on, so it'd mean two ingestion paths for 3.5GB. Right architecture at scale; documented in `docs/signal_handler_notes.md`.
- **Known: two orphaned duplicate companies** in the portal, same pattern both times (predate this project, no domain/tier/signals, shadow the correct signal-sourced record): `Wendy's` (id `441829807307`, correct one is `443738643685`) and, found 2026-08-18 while building Task #8, `Chipotle Mexican Grill` (id `441384846577`, correct signal-sourced one is `443765558499`, has the real live Complaint signal and `bites_tier: Tier 1`). `upsert_signal_company` now adopts+backfills domain-less name matches so this can't recur going forward, but can't fix either existing pair — merging companies is destructive and left for a human. When querying "Chipotle" in this portal, use `443765558499`.
- **UPDATED, 2026-08-19: Task #4 (contact resolution) is reported complete on the OSHA side**, per commit `73d97ce` and `docs/workflow_walkthrough.md` — real contact resolution via the Clay manual-CSV bridge (see the RESOLVED item above), not the originally-planned automated `resolve_contact()` function. That doc is the source of truth for exactly what's live (which contacts, which company, whether it's one demo contact or more) — not re-derived here. The Hiring path's own equivalent (`scripts/populate_hiring_contacts_from_clay.py`) is separately documented in `docs/hiring_workflow_walkthrough.md` Step 7.
- **Task #8 (Tier 1 personalized email) — rules finalized, code built, and the full delivery mechanism verified live end-to-end, including real HubSpot writes.** `docs/task8_email_rules.md` has the full rules (adapted from `Assignment 1/osha_signal/personalize.py`, with the user's explicit deltas: Fat/Cat excluded upstream rather than tone-softened in-prompt; a citation-less opening for Complaint/Accident signals, since both real Tier 1 signals live in this portal are Complaints with no citation yet; a required, reasoned video reference branded per-account via a `{company}` token; 140-word cap up from Assignment 1's 120). `pipeline/personalize.py:draft_first_touch()` calls Claude Sonnet 5 directly (opposite tradeoff from Rung 5's Haiku — this runs once and IS the deliverable, so quality beats cost); real tuning finding: a stated word-count rule alone wasn't enough (155–165 word drafts despite "140, hard ceiling"), fixed by embedding `REFERENCE_DRAFT` in the system prompt as a worked example. **Delivery mechanism** (worked through with the user, since HubSpot Sequences don't support per-contact custom step content via the API — confirmed against HubSpot's docs): the draft is written as a **Note on the Contact** (`pipeline/hubspot_client.py:create_note()` generalized with `NOTE_TO_CONTACT = 202`, verified live), and the contact is auto-enrolled in the dedicated Tier 1 sequence (`QSR T1 - OSHA Trigger - Bites Assignment`, id `846871794`, a To-do step — same `enroll_in_sequence()` mechanism as Task #7) via `pipeline/signal_handler.py:_maybe_draft_and_note_first_touch()`. **Verified live end-to-end** against a real demo contact (John Carothers) with the real Chipotle signal: Note read back exactly as generated, enrollment succeeded. One real HubSpot constraint found: a contact can only be enrolled in one sequence at a time (`SequenceError.CONTACT_ALREADY_ENROLLED` — hit this first against Gordon Penny, already enrolled from Task #7's test). `handle_signal()` now takes `contact: dict | None` (`{"id", "name", "title"}`) instead of a bare `contact_id`, since drafting needs the name/title. Only remaining gap: a real `contact_id` from Task #4.
- **`scripts/build_accounts.py` is superseded but left as-is** — historical artifact of the old account-first Task #3 push (already completed/pushed). The signal-first equivalent is `scripts/handle_signals.py`.
- **Hiring signal — fully built, live-pushed, and largely taken all the way through real contacts, drafts, and sequence enrollment.** Not a placeholder demo: 22 real signals live in HubSpot across 13 real brands, and 11 real contacts fully processed (Note + GDPR consent + confirmed sequence enrollment). Full real-run detail, every real bug hit and fixed, and real HubSpot IDs: **`docs/hiring_workflow_walkthrough.md`** (read this first for Hiring — it supersedes the summary below). Design/scope notes: `docs/hiring_signal_scope.md` (scanner), `docs/hiring_email_rules.md` (copy rules).
  - **Scan** (`pipeline/hiring_scanner.py`, `pipeline/ats_client.py`, `pipeline/hiring_seed.py`): 7 known company boards across Greenhouse/Lever/Workday (Sweetgreen, Caribou Coffee, Blue Bottle Coffee, Insomnia Cookies, Chipotle, Whataburger, Shake Shack) — zero of the 28 QSR50 large chains have a public Greenhouse/Lever board, Workday is what closes that gap. Plus `pipeline/adzuna_client.py`/`pipeline/hiring_industry_classifier.py`: a keyword-based industry-wide discovery layer (the only source that doesn't require already knowing the company), gated on real, working `ADZUNA_APP_ID`/`ADZUNA_APP_KEY` credentials the user created. Two real undocumented Workday API bugs found and fixed (`limit` caps at 20; `total` is only correct on the first page — a naive loop silently truncated Chipotle's scan from 216 to 40 before the fix).
  - **Relevance filter, corrected during planning before it shipped**: any seniority qualifies (not just Director/VP/Head/Chief as first drafted) — seniority belongs at contact resolution, not at what counts as a signal.
  - **Industry-precision filter for Adzuna**: `pipeline/hiring_industry_classifier.py`, a small classifier deliberately kept separate from OSHA's own `tier_classifier.py:classify_tier` (that one's prompt presupposes the input already is a restaurant chain, which is correct for OSHA but was confirmed live to wrongly accept "Marriott Hotels Resorts" when reused for Adzuna's general search — fixing it in place risked regressing OSHA's own verified behavior, so a separate file was built instead; zero changes to `tier_classifier.py`).
  - **Push** (`pipeline/signal_handler.py:handle_hiring_signal`): done, live. 22 real `qsr_signal` records pushed 2026-08-18. Found (not just theorized) a real duplicate-Company race condition pushing multiple signals for the same company back-to-back — 4 of 13 real brands got split into 2 Company records each, cosmetic, left for manual merge.
  - **Contact resolution**: same real constraint as OSHA — Amplemarket never unblocked, and Clay's real-time path needs a paid plan this workspace's trial doesn't have. Resolved via a manual CSV bridge instead (`scripts/export_hiring_companies_csv.py` → user enriches in Clay → `scripts/populate_hiring_contacts_from_clay.py`), same shape as the OSHA side's own Clay workaround. **11 of 16 CSV-enriched contacts are fully live**: real HubSpot Contact, a real generated draft as a Note, GDPR consent recorded, confirmed enrollment in the single "QSR Hiring Signal" sequence (id `847727806` — one sequence for both Tier 1 and Tier 3, an explicit demo-scoping decision, unlike OSHA's two separate sequences).
  - **Tier 1 email copy**: finalized and approved (`docs/hiring_email_rules.md`, `pipeline/hiring_personalize.py`) — the user rewrote the first draft substantially; the real, important correction was that "sell the problem" copy should still name genuine downstream stakes (injuries, quality, compliance) even though the trigger itself is framed as growth/investment, not risk.
  - **What's genuinely still open** (see `docs/hiring_workflow_walkthrough.md`'s closing section for the full list): the finalized email/Note/enrollment logic is **not wired into** `handle_hiring_signal()` itself — every real draft so far ran through the standalone population script, not the routine signal-handling path, so `tier1_first_touch` in that function's return value still says "not yet designed" (stale wording) and a fresh live signal today wouldn't auto-draft anyone even with a real contact. `_maybe_enroll_hiring_tier3` also still only gates on Tier 3 and doesn't reflect the single-sequence decision. 9 of the 13 real brands still have zero contacts.
- An early system-map Artifact (`https://claude.ai/code/artifact/c18827bf-781f-42be-9cc2-56a66c7188e7`) reflects the **old, now-superseded** account-first architecture — stale, not updated. Worth redoing once the signal-first build has real code behind it, but not yet requested.

## Key references

- **Current architecture (read first):** `docs/signal_first_architecture.md`
- **Hiring path — read `docs/hiring_workflow_walkthrough.md` first**: the real, verified, end-to-end run (real IDs, real bugs, real chronology). `docs/hiring_signal_scope.md` (scanner design/discovery findings) and `docs/hiring_email_rules.md` (copy rules, what changed in review) are the supporting design docs behind it.
- Site-count waterfall design: `docs/account_sourcing_methodology.md`
- DOL/OSHA API findings: `docs/dol_api_notes.md`
- osha.gov/ords/imis mechanics (the live scanner data source): `docs/osha_ords_imis_notes.md`
- Signal handler mechanics (HubSpot custom object + Slack findings): `docs/signal_handler_notes.md`
- Rung 5 (LLM tier classification) mechanics + measured coverage: `docs/tier_classifier_notes.md`
- Brand-wide history: why it undercounts, the $0 fix, and the scoped-but-declined real fix: `docs/brand_history_gap.md`
- Task #7 (HubSpot workflow + Tier 3 sequence): scopes needed, why Sequences can't be created via API, the no-native-workflow decision: `docs/task7_workflow_notes.md`
- Task #8 (Tier 1 personalized email): finalized copy rules, the reference draft, prompt-tuning findings, and a real generated draft against Chipotle's live signal: `docs/task8_email_rules.md`
- **OSHA's real Task #4/#9 completion**: commit `73d97ce` ("Complete Task #4/#9: resolve contacts via Clay CSV round-trip, push Chipotle end-to-end") and `docs/workflow_walkthrough.md` — read these directly for the real current OSHA status rather than this doc's older Task #4/#7/#8 bullets above, which predate that commit.
- Clay investigation (historical — explains why Amplemarket was tried first, and why Clay ended up as a manual bridge rather than an automated call): `docs/clay_api_notes.md`
- Original plan (**partially superseded, now annotated with 2026-08-19 supersession notes inline** — Amplemarket→Clay, the Hiring signal-source list, the Hiring Slack/Note template — rather than rewritten, so the historical "why" stays visible): `/Users/ariherskovitz/.claude/plans/task-build-an-automated-parsed-rocket.md`
- Pipeline code: `pipeline/*.py` (config, hubspot_client, slack_client, clay_client, tiering, persona_tracks, accounts_seed, osha_client, signal_scanner, site_count, signal_handler, personalize, ats_client, hiring_seed, hiring_scanner, adzuna_client, hiring_industry_classifier, hiring_personalize), `scripts/*.py`
- GitHub remote: `https://github.com/gadiherskovitz-boop/bites-osha-2.git` — **`git push` is currently broken** (credential error: `"could not read Username for 'https://github.com'"`, not a code problem) with **7 real commits sitting locally unpushed** as of this handoff (`73d97ce` down through `54074e1`, spanning both this session's Hiring work and the parallel session's OSHA Task #4/#9 completion). The user needs to refresh their GitHub credentials (new Personal Access Token, since GitHub requires one instead of a password for git operations) before any of this reaches the remote.
- Working directory: `/Users/ariherskovitz/Documents/Claude/Projects/Bites Assignment/Assignment 2`
- Task tracker: 9 tasks (#1–#9); **as of this handoff, #1–#8 are all complete or code-complete-and-live-verified, and #4/#9 are reported complete via the Clay manual-CSV bridge** (see the OSHA reference above — read that commit/doc for the real specifics, not re-derived here). Hiring signal (no task number in the original 9) is also complete in the same shape: scanner, tiering, push, and contact population all live and verified (`docs/hiring_workflow_walkthrough.md`) — the one real gap is that the Hiring Tier 1 email/enrollment logic isn't wired into the automated `handle_hiring_signal()` path itself (see the Hiring bullet above). This session's local `TaskCreate`/`TaskList` tracker was empty at session start (not carried over) — only tracks work done in-session, not the full #1–#9 backlog; treat this doc as the source of truth for overall status.

## Suggested next steps

**Hiring path — the automation gap, not new building:**
1. Wire the finalized email rules (`pipeline/hiring_personalize.py`) into `pipeline/signal_handler.py:handle_hiring_signal` — mirror OSHA's `_maybe_draft_and_note_first_touch()` pattern. Right now `tier1_first_touch` always reports "not yet designed" even though the copy is done; every real draft so far went through the standalone `scripts/populate_hiring_contacts_from_clay.py`, not the routine signal-handling path.
2. Fix `_maybe_enroll_hiring_tier3` to reflect the real decision made 2026-08-19: **one shared sequence** ("QSR Hiring Signal", id `847727806`) for both Tier 1 and Tier 3, not a Tier-3-only gate pointing at a separate `HUBSPOT_HIRING_TIER3_SEQUENCE_ID`.
3. Get contacts for the 9 real Hiring brands that still have none (DIG INN Support, McDonald's, Abby's Pizza, Steak 'n Shake, Dunkin', Yoshinoya Companies, Otg, Mendocino Farms, and Popeyes needs a real email) — another round of the export→Clay→import loop.
4. Merge the 4 duplicate-Company pairs from the Hiring push's race condition (DIG INN Support, Steak 'n Shake, Lettuce Entertain You Restaurants, Otg) — cosmetic, manual, in the HubSpot UI.

**Cross-cutting, both paths:**
5. **Fix `git push`** — a GitHub credential issue (expired/invalid token), not a code problem. 7 real commits are waiting locally. Needs the user to generate a new Personal Access Token and re-authenticate (see the GitHub remote note above).
6. **Sales Hub Enterprise trial had 3 days left as of 2026-08-18** — check whether it's been extended/converted before presenting; Sequences/Workflows access depends on that plan tier, and both paths now rely on real sequence enrollment.
7. Known cosmetic duplicate-company pairs to decide on before presenting: OSHA's pre-existing Wendy's/Chipotle/McDonald's/Chick-fil-A orphans (predate this project) plus the 4 new Hiring-push duplicates above.
8. Before presenting: decide with the user how much of the 268-signal OSHA backlog and the 20-signal Hiring backlog to actually keep pushed/visible — both scanners will keep finding more on every fresh run, and how much volume to show live is a presentation judgment call, not a default.

**Read first, don't re-derive**: `docs/hiring_workflow_walkthrough.md` (Hiring, this session) and `docs/workflow_walkthrough.md` + commit `73d97ce` (OSHA, the parallel session) both already contain thorough, live-verified answers to "what actually works right now" — worth reading in full before assuming anything needs rebuilding.

## Suggested skills

- `karpathy-coding-guidelines` — already the agreed coding standard; keep applying (minimal diffs, no speculative abstraction) as the signal-first rebuild proceeds.
